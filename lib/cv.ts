/**
 * The CV asset and the name it saves as. Single source for every download
 * control, so the two never drift apart.
 */
export const CV_PATH = "/portfolio.pdf";
export const CV_FILENAME = "youssefbushra.pdf";
